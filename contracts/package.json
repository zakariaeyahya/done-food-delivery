{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Ce dossier contient tous les smart contracts Solidity qui constituent le cœur métier de la plateforme DONE Food Delivery. Les contrats sont déployés sur le réseau Polygon Mumbai (testnet).",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:mumbai": "hardhat run ../scripts/deploy-all.js --network mumbai",
    "setup-roles:mumbai": "hardhat run ../scripts/setup-roles.js --network mumbai",
    "deploy:local": "hardhat run ../scripts/deploy-all.js --network hardhat"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "dotenv": "^16.6.1",
    "@openzeppelin/contracts": "^5.0.0",
    "ethers": "^6.0.0"
  },
  "devDependencies": {
    "hardhat": "^2.19.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0"
  }
}
